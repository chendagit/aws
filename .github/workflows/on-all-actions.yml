name: On all actions
on:
  workflow_dispatch:
  pull_request:
  schedule:
    - cron:  '56 8 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run a one-line script
        run: echo 'Hello, world!'